import { types } from "../types/types"


export const calcReducer = (state={}, action) => {
  
    switch(action.type){

        case types.suma:
            return{

            };

        case types.resta:
            return{
                
            }

        case types.igual:
            return{

            }


    }
}
